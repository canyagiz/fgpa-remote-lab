# Managed by the provisioning wizard. Mode 0600. Holds this shuttle's
# bearer token, injected at provision time and never written to any repo.
# The systemd unit reads it via EnvironmentFile; the env var takes
# precedence over agent.conf.
FPGA_AGENT_TOKEN={{ agent_token }}
FPGA_AGENT_MASTER_URL={{ portal_master_url }}
