# Copy to hosts.ini. One line per shuttle; the details of each live in
# inventory/host_vars/<name>.yml. The wizard writes this file for you.

[shuttles]
shuttle13

# ansible_host, ansible_user and the hardware map are all in
# host_vars/shuttle13.yml. The SSH password is passed at run time
# (--ask-pass or the wizard form), never written here.
